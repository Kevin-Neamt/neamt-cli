# Runtime dependencies for the bundled Neamt engine.
# Kept in sync with pyproject.toml [project.dependencies]. Used by packaged
# desktop builds to provision the bundled Python interpreter.
fastapi>=0.110
uvicorn>=0.29
typer>=0.12
rich>=13.0
pydantic>=2.0
httpx>=0.27
websockets>=12.0
cryptography>=42.0
anthropic>=0.25
